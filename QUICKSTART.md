# ⚡ Быстрый старт

## 1️⃣ Установка зависимостей

```bash
# Backend
cd backend
npm install

# Frontend (в новом терминале)
cd frontend
npm install
```

## 2️⃣ Настройка окружения

### Backend (.env)
```bash
cd backend
cp .env.example .env
```

Отредактируйте `.env`:
```env
DATABASE_URL=postgresql://user:password@localhost:5432/java_interview_tinder
BOT_TOKEN=your_bot_token_from_botfather
OPENROUTER_API_KEY=your_key  # Опционально
PORT=3000
NODE_ENV=development
```

### Frontend (.env)
```bash
cd frontend
cp .env.example .env
```

Отредактируйте `.env`:
```env
VITE_API_URL=http://localhost:3000/api
```

## 3️⃣ База данных

### Вариант A: PostgreSQL локально

```bash
# Создайте БД
createdb java_interview_tinder

# Инициализируйте таблицы
cd backend
npm run init-db

# Заполните данными
npm run seed-db
```

### Вариант B: Используйте SQL файл

```bash
psql java_interview_tinder < database/schema.sql
```

## 4️⃣ Запуск

### Терминал 1 - Backend
```bash
cd backend
npm run dev
```

Должно появиться:
```
╔════════════════════════════════════════════════╗
║   🚀 Java Interview Tinder Backend Started   ║
╠════════════════════════════════════════════════╣
║   Port: 3000                                   ║
║   Mode: Development                            ║
║   Database: ✅ Connected                      ║
║   OpenRouter: ✅ Configured                   ║
╚════════════════════════════════════════════════╝
```

### Терминал 2 - Frontend
```bash
cd frontend
npm run dev
```

Откройте: http://localhost:5173

## 5️⃣ Настройка Telegram бота (для продакшена)

1. Создайте бота через [@BotFather](https://t.me/BotFather):
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните токен

2. Настройте Menu Button:
   - `/setmenubutton`
   - Выберите вашего бота
   - Текст: "🚀 Открыть приложение"
   - URL: ваш deployed frontend URL

## 🎉 Готово!

Приложение запущено и работает локально.

## 📋 Чеклист

- [ ] Node.js установлен (v18+)
- [ ] PostgreSQL установлен и запущен
- [ ] База данных создана
- [ ] .env файлы настроены
- [ ] npm install выполнен
- [ ] Backend запущен (порт 3000)
- [ ] Frontend запущен (порт 5173)
- [ ] Приложение открывается в браузере

## 🐛 Проблемы?

### Backend не запускается
- Проверьте что PostgreSQL запущен: `psql -l`
- Проверьте DATABASE_URL в .env
- Убедитесь что база создана: `createdb java_interview_tinder`

### Frontend не подключается
- Проверьте что Backend работает: `curl http://localhost:3000/health`
- Проверьте VITE_API_URL в .env
- Очистите кэш: `rm -rf node_modules/.vite`

### База данных
- Ошибка подключения: проверьте credentials в DATABASE_URL
- Таблицы не созданы: запустите `npm run init-db`
- Нет вопросов: запустите `npm run seed-db`

## 📚 Следующие шаги

1. Прочитайте [README.md](README.md) для полной документации
2. Прочитайте [DEPLOYMENT.md](DEPLOYMENT.md) для инструкций по деплою
3. Кастомизируйте вопросы в `backend/src/scripts/seed-db.js`
4. Измените дизайн в `frontend/src/components/*.css`

## 💡 Полезные команды

```bash
# Backend
npm run dev      # Запуск с hot reload
npm start        # Продакшн запуск
npm run init-db  # Создать таблицы
npm run seed-db  # Заполнить вопросами

# Frontend
npm run dev      # Dev сервер
npm run build    # Сборка для продакшена
npm run preview  # Просмотр production build
```
