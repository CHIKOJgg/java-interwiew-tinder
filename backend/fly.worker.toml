app = "interview-tinder-worker"
primary_region = "fra"

[build]

[env]
  NODE_ENV = "production"

[processes]
  worker = "node src/worker.js"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  processes = ["worker"]