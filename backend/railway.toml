[build]
  dockerfile = "Dockerfile"

[deploy]
  restart_policy_type = "ON_FAILURE"
  restart_policy_max_retries = 5

[env]
  NODE_ENV = "production"
  PORT = "10000"
  ALLOWED_ORIGINS = "http://localhost:3000,http://localhost:5173,https://solve-it-rho.vercel.app"
