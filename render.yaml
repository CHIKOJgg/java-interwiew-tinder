# DEPRECATED — Render Blueprint (legacy).
# This project now deploys the backend to Railway.
# See DEPLOY.md and railway.toml for Railway setup instructions.
# See DEPLOY_RENDER.md for the previous Render deployment reference.